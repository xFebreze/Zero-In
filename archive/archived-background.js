/*chrome.runtime.onInstalled.addListener(() => {
  chrome.storage.sync.set({});
  console.log('Not Wrong');
});*/

/*const fs = require('fs');
const Blocked = chrome.storage;
const StudyPage = "https://thenextweb.com/news/13-simple-ways-to-stay-efficient-while-you-work";
const website;

let websites = ["",""];*/


/*var blockRule = {
  conditions: [
    new chrome.declarativeContent.PageStateMatcher({
      pageUrl: { hostEquals: 'www.powerlanguage.co.uk'},
    })
  ],
  actions: [ new chrome.declarativeContent.ShowAction() ]
};

chrome.runtime.onInstalled.addListener(function(details) {
  chrome.declarativeContent.onPageChanged.removeRules(undefined, function() {
    chrome.declarativeContent.onPageChanged.addRules([blockRule]);
  });
});*/


/*chrome.runtime.onInstalled.addListener(() => {
  chrome.storage.sync.set({key: value}, function() {
  console.log("Imported default blocked websites");
  });
});
*/